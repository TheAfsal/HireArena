{
  "extends": "../tsconfig.json", // Extend the root tsconfig.json
  "compilerOptions": {
    "outDir": "./dist/gateway-service" // Service-specific output directory
  },
  "include": [
    "src/**/*.ts" // Only include .ts files in the src directory
, "src/app.ts", "../server.ts"  ]
}
