import { Request, Response } from "express";

export interface IMachineTaskController {
  // getMachineTaskByJobId(req: Request, res: Response): Promise<void>;
  // getMachineTaskDetails(req: Request, res: Response): Promise<void>;
  // startTask(req: Request, res: Response): Promise<void>;
  // checkSubmission(req: Request, res: Response): Promise<void>;
  // submitProject(req: Request, res: Response): Promise<void>;
}